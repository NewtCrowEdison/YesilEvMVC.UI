@model YesilEvMVC.UI.ViewModels.UreticiViewModel
@{
    ViewBag.Title = "Üretici Düzenle";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container mt-5">
    <h2 class="text-warning text-center mb-4">✏️ Üretici Düzenle</h2>

    <div class="row justify-content-center">
        <div class="col-md-6">
            <form method="post" asp-action="UreticiDuzenle" class="border p-4 rounded shadow-sm bg-light">
                <input type="hidden" asp-for="Id" />

                <div class="mb-3">
                    <label asp-for="UreticiAdi" class="form-label">Üretici Adı</label>
                    <input asp-for="UreticiAdi" class="form-control" />
                    <span asp-validation-for="UreticiAdi" class="text-danger"></span>
                </div>

                <div class="text-end">
                    <button type="submit" class="btn btn-warning">
                        <i class="bi bi-pencil-square me-1"></i> Güncelle
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>
