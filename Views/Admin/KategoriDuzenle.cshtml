@model YesilEvMVC.UI.ViewModels.KategoriVM
@{
    ViewBag.Title = "Kategori Düzenle";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container mt-5">
    <h2 class="text-warning text-center mb-4">✏️ Kategori Düzenle</h2>

    <div class="row justify-content-center">
        <div class="col-md-6">
            <form method="post" asp-action="KategoriDuzenle" class="border p-4 rounded shadow-sm bg-light">
                <input type="hidden" asp-for="Id" />

                <div class="mb-3">
                    <label asp-for="KategoriAdi" class="form-label">Kategori Adı</label>
                    <input asp-for="KategoriAdi" class="form-control" />
                    <span asp-validation-for="KategoriAdi" class="text-danger"></span>
                </div>

                <div class="text-end">
                    <button type="submit" class="btn btn-warning">
                        <i class="bi bi-pencil-square me-1"></i> Güncelle
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>
