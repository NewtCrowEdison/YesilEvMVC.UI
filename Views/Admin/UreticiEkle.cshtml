@model YesilEvMVC.UI.ViewModels.UreticiVM
@{
    ViewBag.Title = "Üretici Ekle";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container mt-5">
    <h2 class="text-success text-center mb-4">🏭 Yeni Üretici Ekle</h2>

    <div class="row justify-content-center">
        <div class="col-md-6">
            <form method="post" asp-action="UreticiEkle" class="border p-4 rounded shadow-sm bg-light">
                <div class="mb-3">
                    <label asp-for="UreticiAdi" class="form-label">Üretici Adı</label>
                    <input asp-for="UreticiAdi" class="form-control" />
                    <span asp-validation-for="UreticiAdi" class="text-danger"></span>
                </div>

                <div class="text-end">
                    <button type="submit" class="btn btn-success">
                        <i class="bi bi-plus-circle me-1"></i> Ekle
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>
